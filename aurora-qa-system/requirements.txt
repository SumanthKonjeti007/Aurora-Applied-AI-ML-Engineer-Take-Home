# ============================================
# PRODUCTION REQUIREMENTS
# All dependencies required for production deployment
# ============================================

# Core API Framework
fastapi==0.115.0
uvicorn[standard]==0.24.0
pydantic>=2.5.0
python-multipart==0.0.6

# HTTP Client
requests==2.31.0
httpx>=0.27.0

# Data Processing
numpy==1.26.2
# pandas removed - not used in production API

# LLM - Mistral AI
mistralai==1.9.11

# Search & Retrieval
rank-bm25==0.2.2

# Knowledge Graph
networkx==3.4.2

# Vector Search - Qdrant Cloud (NO LOCAL FAISS)
qdrant-client>=1.7.1

# Embeddings (REQUIRED for runtime query encoding in QdrantSearch)
sentence-transformers==2.2.2
torch==2.2.0

# Utilities
python-dotenv==1.0.0
tqdm==4.66.1
datefinder==0.7.3
python-dateutil==2.8.2

# ============================================
# REMOVED FOR PRODUCTION:
# ============================================
# - faiss-cpu (using Qdrant Cloud instead)
# - pandas (not used in production API)
# - spacy, gliner (only used in preprocessing scripts)
# - pytest, black (development only)
# ============================================
